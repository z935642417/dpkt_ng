# L2TP Protocol — Implementation Plan

> **Goal:** Create `dpkt/l2tp.py` with L2TPv2/v3 parsing: unified header, 12 control message types, 15 core AVPs, Vendor AVP.

**Architecture:** `L2TP(dpkt.Packet)` with `__bit_fields__` for T/L/S flags, `_msg_sw` for control message dispatch, `_avp_sw` for AVP type dispatch. Single file ~500 lines.

---

## File Map

| File | Action |
|------|--------|
| `dpkt/l2tp.py` | **Create** |
| `dpkt/__init__.py` | +1 import |

---

### Task 1: L2TP Header + Data Message

Create `dpkt/l2tp.py`:

```python
"""L2TP protocol."""
from __future__ import absolute_import, print_function
import struct
from . import dpkt

# Message types (in AVP 0)
L2TP_MSG_SCCRQ=1; L2TP_MSG_SCCRP=2; L2TP_MSG_SCCCN=3; L2TP_MSG_STOPCCN=4
L2TP_MSG_HELLO=6; L2TP_MSG_OCRQ=7; L2TP_MSG_OCRP=8; L2TP_MSG_OCCN=9
L2TP_MSG_ICRQ=10; L2TP_MSG_ICRP=11; L2TP_MSG_ICCN=12; L2TP_MSG_CDN=14
L2TP_MSG_WEN=15; L2TP_MSG_SLI=16

# AVP types
AVP_MSG_TYPE=0; AVP_RESULT_CODE=1; AVP_PROTO_VER=2; AVP_HOST_NAME=7
AVP_FRAMING_CAP=3; AVP_BEARER_CAP=4; AVP_TIE_BREAKER=5
AVP_FIRMWARE_REV=6; AVP_VENDOR_NAME=8; AVP_ASSIGNED_TUNNEL=9
AVP_RECEIVE_WINDOW=10; AVP_CHALLENGE=11; AVP_CHALLENGE_RESP=13
AVP_ASSIGNED_SESSION=14; AVP_CALL_SERIAL=15; AVP_CALLING_NUMBER=22
AVP_CTRL_MSG=0x3c # Vendor-Specific
```

Add `L2TP` header + data message + test. Commit: `feat: add L2TP header and data message parsing`

### Task 2: AVP Framework + 15 Core AVPs

Add `L2TPAVP` base + typed subclasses + `_avp_sw` dispatch. Commit: `feat: add L2TP AVP framework with 15 core types`

### Task 3: Control Messages + Dispatch

Add `L2TPControl` + 12 message subclasses + `_msg_sw` dispatch + `__init__.py` import. Commit: `feat: add L2TP control message types and dispatch`

---

**Total:** 3 tasks, ~500 lines, ~10 tests. Expected ~575 passed.
